<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sports.auth.mapper.UserMapper">

    <select id="selectUserPage" resultType="com.sports.auth.entity.User">
        SELECT * FROM t_user
        WHERE deleted = 0
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="realName != null and realName != ''">
            AND real_name LIKE CONCAT('%', #{realName}, '%')
        </if>
        <if test="role != null">
            AND role = #{role}
        </if>
        <if test="college != null and college != ''">
            AND college = #{college}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectUserVOListByIds" resultType="com.sports.auth.entity.vo.UserVO">
        SELECT id, username, real_name, college, class_name, role
        FROM t_user
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </select>

</mapper>
