<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sports.sports.mapper.RegistrationMapper">

    <select id="selectRegistrationPage" resultType="com.sports.sports.entity.Registration">
        SELECT r.*,
               e.name AS eventName,
               m.name AS meetingName
        FROM t_registration r
        LEFT JOIN t_event e ON r.event_id = e.id
        LEFT JOIN t_meeting m ON r.meeting_id = m.id
        WHERE r.deleted = 0
        <if test="meetingId != null">
            AND r.meeting_id = #{meetingId}
        </if>
        <if test="eventId != null">
            AND r.event_id = #{eventId}
        </if>
        <if test="userId != null">
            AND r.user_id = #{userId}
        </if>
        <if test="status != null">
            AND r.status = #{status}
        </if>
        ORDER BY r.create_time DESC
    </select>

    <select id="countByEventId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_registration
        WHERE event_id = #{eventId} AND status = 1 AND deleted = 0
    </select>

    <select id="selectRegistrationStats" resultType="java.util.Map">
        SELECT e.name AS event_name,
               COUNT(*) AS total,
               SUM(CASE WHEN r.status = 0 THEN 1 ELSE 0 END) AS pending,
               SUM(CASE WHEN r.status = 1 THEN 1 ELSE 0 END) AS approved,
               SUM(CASE WHEN r.status = 2 THEN 1 ELSE 0 END) AS rejected,
               SUM(CASE WHEN r.status = 3 THEN 1 ELSE 0 END) AS cancelled
        FROM t_registration r
        LEFT JOIN t_event e ON r.event_id = e.id
        WHERE r.deleted = 0
        <if test="meetingId != null">
            AND r.meeting_id = #{meetingId}
        </if>
        GROUP BY r.event_id, e.name
        ORDER BY total DESC
    </select>

    <select id="selectCollegeRegistrationStats" resultType="java.util.Map">
        SELECT r.user_id,
               COUNT(*) AS total
        FROM t_registration r
        WHERE r.deleted = 0 AND r.status = 1
        <if test="meetingId != null">
            AND r.meeting_id = #{meetingId}
        </if>
        GROUP BY r.user_id
    </select>

</mapper>
